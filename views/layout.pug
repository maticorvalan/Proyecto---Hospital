doctype html
html(lang="es" class="h-full")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= title || 'Sistema HIS'
    //- Enlaza el CSS compilado por Tailwind
    link(rel="stylesheet", href="/css/styles.css")
  body(class="flex flex-col min-h-screen bg-gradient-to-b from-gray-50 via-emerald-50 to-blue-400 bg-fixed")
    header(class="relative flex flex-wrap w-full text-sm py-3 shadow-lg shadow-blue-200")
      include partials/header

    main.mx-auto.p-4.grow
      block content

    footer.mx-auto
      include partials/footer
      